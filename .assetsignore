# Lo que NO se publica en clicketerno.com.ar.
# Cloudflare sube TODO lo que está en la carpeta del repo: sin este archivo,
# cualquiera podía bajarse sql/claves.sql desde la web.
sql/
pruebas/
apps-script/
*.md
wrangler.json
.assetsignore
